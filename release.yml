tag: v1.1.8
name: "Version 1.1.8"
body: |
  ✨ News :
  - ADD Offline S504 after power outage, shutdown, or tablet crash
  - ADD deaf notification except for MQTT crashes
  - ADD HA JSON template for sensor
  - ADD LWT to mqtt service


  🐛 Bug Fix :
  - Fix error Temperature sensor
  - Optimize json mqtt + encode to Led Service
  - Optimize json mqtt to IO service + disabled by default

  ✨ Next Release :
  - ADD more translation
  - REMOVE ScaffoldMessenger
apk_path: build/app/outputs/flutter-apk/app-release.apk